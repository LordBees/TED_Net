##import
##file for network stuff
import BeeLibv3 as Blib

def scrapefrontpage():
  pass
def is_valid(link):
    pass
def recurselooper(x,dat):
    pass
def scrape_all(xurl,charset,linklen):
  for x in range(0,linklen):
    for y in range(0,len(charset)):
      ##xurl
        link = xurl+0

def getlinksvalid(type,linknum):
  if linknum == -1:
    ##get all
  else:
    get specific
    
 class linkcache:
  ##attr
  linktype2op = ""
  cachex = []
  
  ##meth
  def __init__(linktype2op):
    self.linktype2op = linktype2op
  
  def cacheall():
    if len(cachex)!=0:
      cachex = []
    
    for x in range(len)
  def cachelink():
        pass
